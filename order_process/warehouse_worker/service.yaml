apiVersion: v1
kind: Service
metadata:
  name: warehouse-worker
spec:
  clusterIP: None
  selector:
    pass.subject: warehouse-worker
  ports:
    - name: input-pool
      port: 8080
      targetPort: 8080
